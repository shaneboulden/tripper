<script>
function initialize() {
  var mapProp = {
      center:new google.maps.LatLng(<%= @geo_lat %>,<%= @geo_long %>),
      zoom:6,
      mapTypeId:google.maps.MapTypeId.HYBRID
   };
  var map=new
      google.maps.Map(document.getElementById("googleMap"),mapProp);

    var marker = new google.maps.Marker({
position: new google.maps.LatLng(<%= @geo_lat %>,<%= @geo_long %>),
      map: map,
      title: 'hello, world!'
      });
}
google.maps.event.addDomListener(window, 'load', initialize);
</script>
<p> There are <%= @geotweets.size %> tweets! </p>
<% @geotweets.each do |tweet| %>
    <p><b>Text:</b> <%= tweet.text %> </p>
    <p><b>URI: </b> <%= tweet.uri %></p>
    <p><b>user: </b>
<% end %>
<div id="googleMap" style="width:640px;height:360px;"></div>
